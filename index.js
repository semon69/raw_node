// Local modules
const {a, add} = require('./local-1')
const {a: a2, add: add2} = require('./local-2')

// console.log(a2, add2(2, 3, 1));

// built in modules

const path = require("path")
console.log(path.dirname("F:/PH Level 2/Node and Express/raw_node/index.js"));